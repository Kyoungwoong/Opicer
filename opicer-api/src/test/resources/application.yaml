spring:
  datasource:
    url: ${OPICER_TEST_DB_URL:jdbc:postgresql://localhost:5432/opicer_test}
    driver-class-name: org.postgresql.Driver
    username: ${OPICER_DB_USER:opicer}
    password: ${OPICER_DB_PASSWORD:opicer}
  jpa:
    hibernate:
      ddl-auto: create-drop
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: dummy
            client-secret: dummy
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            scope:
              - profile_nickname
              - account_email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

opicer:
  auth:
    jwt-secret: test-secret-test-secret-test-secret-32bytes
    jwt-ttl-seconds: 3600
    cookie-name: OPICER_AUTH
    cookie-secure: false
    admin-allowlist: ""
    frontend-base-url: http://localhost:3000
